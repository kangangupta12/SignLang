# Models Directory

Trained checkpoints and exported SavedModels will be saved here.

- models/checkpoints/ - best .ckpt weights from training.
- models/saved_model/ - optional TF SavedModel exports for serving.

Add .gitignore rules as needed to prevent large binaries from being committed.
